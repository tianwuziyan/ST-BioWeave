export function sortRelatives(items=[]) { return [...items].sort((a,b)=>(a.generation??0)-(b.generation??0)||String(a.birth_story_time??'').localeCompare(String(b.birth_story_time??''))||(a.birth_floor??Infinity)-(b.birth_floor??Infinity)||(a.relationship_created_floor??Infinity)-(b.relationship_created_floor??Infinity)||String(a.character_id).localeCompare(String(b.character_id))); }
export function childrenOf(id,relationships=[]) { return sortRelatives(relationships.filter(r=>r.parent_id===id).map(r=>({...(r.child??{}),character_id:r.child_id,generation:1,relationship_created_floor:r.created_floor}))); }
